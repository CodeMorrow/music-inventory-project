<table class="table">
      <tr class="table__row">
        <th class="table__header">Song</th>
        <th class="table__header">Artist</th> 
        <th class="table__header">Album</th>
        <th class="table__header">Genre</th>
        <th class="table__header">Minutes</th> 
        <th class="table__header">Seconds</th>
        <th class="table__header">Song Rating</th>
        <th class="table__header">Album Rating</th> 
      </tr>
  <% x.each do |l|
      l.each do |title,album,artist,genre,minutes,seconds,songrating,albumrating| %>
  <tr class="table__row">
  	<td class="table__data"><%= title %></td>
  	<td class="table__data"><%= album %></td>
  	<td class="table__data"><%= artist %></td>
  	<td class="table__data"><%= genre %></td>
  	<td class="table__data"><%= minutes %></td>
  	<td class="table__data"><%= seconds %></td>
  	<td class="table__data"><%= songrating %></td>
  	<td class="table__data"><%= albumrating %></td>
   </tr>
   <% end %>
   <% end %>
</table>











<!-- Added the below code for table rendering -->
 <% #= table 


	# @tbl_header = "<thead><tr>
 #    <th>Title</th>   <th>Artist</th>    <th>Album</th>        <th>Genre</th>
 #    <th>Minutes</th>  <th>Seconds</th>  <th>Song Rating</th>  <th>Album Rating</th> 
 #    </tr></thead><tbody>"
	
	# # binding.pry
	# if File.exist?("music_db.txt")
	#   table = @x.map do |portion|
	# 	    "<table>\n" << @tbl_header << "\n<tr>" << portion.map do |column|
		      
	# 	      "<td>" << column.map do |element|
	# 	        element.to_s
	# 	      end.join("</td><td>") << "</td>"
	# 	    end.join("</tr>\n<tr>") << "</tr>\n</tbody>\n</table>\n"
	# 	  end.join("\n")
	#    return table
	# end

 %>

 